(function (angular) {
    'use strict';
    angular.module('equipMe.services')
        .service('registerService', registerService);

    function registerService() {
        var vm = this;
    }
}(angular));