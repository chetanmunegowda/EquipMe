'use strict';

describe('equipMe.prodCart', function() {

  beforeEach(module('equipMe.prodCart'));

  describe('products Cart controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var prodCartCtrl = $controller('prodCartController');
      expect(registerCtrl).toBeDefined();
    }));

  });
});
