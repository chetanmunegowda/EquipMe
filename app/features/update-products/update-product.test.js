'use strict';

describe('equipMe.updateProduct', function() {

    beforeEach(module('equipMe.updateProduct'));

    describe('update product controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var updateProductCtrl = $controller('updateProductController');
            expect(updateProductCtrl).toBeDefined();
        }));

    });
});
