'use strict';

describe('equipMe.productsDesc', function() {

  beforeEach(module('equipMe.productsDesc'));

  describe('products Description controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var productsCtrl = $controller('productsDescriptionController');
      expect(registerCtrl).toBeDefined();
    }));

  });
});
