(function (angular) {
    'use strict';

    angular
        .module('equipMe.productsDesc')
        .controller('productsDescController', productsDescController);

    function productsDescController(productService, $filter, ngCart) {
        var vm = this;

    }
}(angular));