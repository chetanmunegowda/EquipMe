'use strict';

describe('equipMe.register', function() {

  beforeEach(module('equipMe.register'));

  describe('register controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var registerCtrl = $controller('registerController');
      expect(registerCtrl).toBeDefined();
    }));

  });
});
