(function (angular) {
    'use strict';

    angular
        .module('equipMe.register')
        .controller('registerController', registerController);

    function registerController() {
        var vm = this;
    }
}(angular));
